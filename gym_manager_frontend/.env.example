# Environment example for Gym Manager Frontend
# Copy this file to `.env` and fill in values as appropriate.
# Do NOT commit your actual secrets.

# ------------------------------------------------------------------------------
# Vite (recommended)
# ------------------------------------------------------------------------------
# If your frontend uses Vite, use these keys:
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# ------------------------------------------------------------------------------
# Create React App (CRA)
# ------------------------------------------------------------------------------
# If your frontend uses CRA, use these keys instead:
REACT_APP_SUPABASE_URL=https://your-project-ref.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-supabase-anon-key

# Notes:
# - Only one set (Vite OR CRA) is required; the code will auto-detect which to use.
# - Keep your ANON KEY secure; do not commit real values to source control.
# - For Supabase email redirects (if implemented elsewhere), ensure the site URL
#   is configured appropriately in your deploy environment.
