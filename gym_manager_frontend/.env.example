# Frontend environment variables

# Base URL for the real API when mocks are disabled (no trailing slash)
REACT_APP_API_BASE_URL=https://api.example.com

# Toggle MSW mock mode (true|false). Default is false to use real Supabase/services.
REACT_APP_USE_MOCKS=false

# Optional feature flags JSON string
REACT_APP_FEATURE_FLAGS={}

# Supabase configuration (required for real auth).
# IMPORTANT: These must be prefixed with REACT_APP_ for CRA to expose them to the browser.
REACT_APP_SUPABASE_URL=https://YOURPROJECTID.supabase.co
REACT_APP_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6Ikp...
