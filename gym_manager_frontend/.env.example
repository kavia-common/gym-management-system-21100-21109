# Gym Manager Frontend - Environment Variables Example
# Copy this file to ".env" and adjust values for your environment.

# Base URL for the backend API (no trailing slash). Example: https://api.example.com
REACT_APP_API_BASE_URL=http://localhost:4000

# Enable UI mock data mode for development (true/false).
# When true, some pages will use in-memory demo data and bypass network calls.
REACT_APP_USE_MOCKS=true

# Optional JSON string to toggle features on/off.
# Example:
# REACT_APP_FEATURE_FLAGS={"payments":true,"analytics":true}
REACT_APP_FEATURE_FLAGS={}
