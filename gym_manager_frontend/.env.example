# Create React App environment variables for Gym Manager Frontend
# Copy this file to .env.local (do not commit your .env.local)

# Supabase project URL (from Supabase Dashboard > Settings > API)
REACT_APP_SUPABASE_URL=https://YOURPROJECTID.supabase.co

# Supabase anon public key (from Supabase Dashboard > Settings > API)
# IMPORTANT: Ensure no leading/trailing spaces or newlines.
REACT_APP_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1...

# Optional: API base URL for your backend (if using real API instead of mocks)
REACT_APP_API_BASE_URL=

# Set to "true" to enable mock API using MSW for local development
REACT_APP_USE_MOCKS=true

# Notes:
# - CRA only exposes variables prefixed with REACT_APP_.
# - If previously using REACT_APP_SUPABASE_KEY, prefer REACT_APP_SUPABASE_ANON_KEY.
# - After editing .env.local, restart the dev server to apply changes.
